/**
 * concat(a, b)
 * ----------
 * Creates a new array with all the elements from
 * array a and array b. Return the new array with all
 * elements.
 *
 * concat([1, 2], [3, 4]) ==> [1, 2, 3, 4]
 * concat([], [1, 2, 3]) ==> [1, 2, 3]
 * concat(['a', 'b', 'c'], ['a', 'b']) ==> ['a', 'b', 'c', 'a', 'b']
 */
function concat(a, b) {

}

module.exports = concat;
